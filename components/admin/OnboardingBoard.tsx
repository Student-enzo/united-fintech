'use client'

// OnboardingBoard — merchant acquisition/onboarding kanban pipeline.
// Extracted from app/admin/onboarding-crm/page.tsx so it can be imported
// as a standalone component (e.g. tested, lazy-loaded, or reused).

export { default } from '@/app/admin/onboarding-crm/page'
